1. Don't forget to fill the .env file
2. Add your service account key file to cloudStorage.js (to upload image to cloud bucket)
3. Run "npm install" to install the dependencies.

NodeJs used : v16.19.1
